// import { colors } from '../../themes/Colors';
import { Fonts } from '../../themes/Fonts';

export const styles = {
    input: {
        width: '100%',
        marginBottom: 10,
        fontFamily: Fonts.LatoLight,
    },
};